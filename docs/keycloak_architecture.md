# Архитектура и интеграция Keycloak

## Цель
Единая аутентификация/SSO для доступа к PAM‑шлюзу с федерацией в AD/LDAP.

## Компоненты
- Keycloak (IdP) в on‑prem контуре.
- Федерация пользователей из AD/LDAP.
- PAM‑шлюз как OIDC/SAML‑клиент.
- Админ‑группы и роли в Keycloak.

## Потоки аутентификации (MVP)
1) Пользователь входит в PAM‑шлюз.
2) Редирект в Keycloak, аутентификация через AD/LDAP.
3) Keycloak выдаёт ID/Access токены.
4) PAM‑шлюз валидирует токены и применяет RBAC/JIT.

## Маппинг ролей
- В AD создаются группы доступа (например, PAM_Admin, PAM_Auditor, PAM_WinAdmin).
- Keycloak делает групповой маппинг в токены (claim `groups`).
- PAM‑шлюз сопоставляет `groups` → роли/политики.

## Требования к токенам
- `sub`, `email`, `preferred_username`, `groups`.
- Время жизни Access Token: 5–15 мин.
- Refresh Token: 8–12 часов (по политике ИБ).

## Безопасность
- Обязательная MFA на уровне Keycloak.
- Ограничение по IP/сегменту (при необходимости).
- Аудит логинов и сессий в SIEM.

## Выходные артефакты
- Схема ролей и групп.
- Конфигурация клиента PAM в Keycloak.
- Политики MFA и сроков токенов.
