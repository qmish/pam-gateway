{{- if .Values.keycloak.pdb.enabled }}
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: keycloak-pdb
  namespace: {{ .Values.namespace }}
spec:
  minAvailable: {{ .Values.keycloak.pdb.minAvailable }}
  selector:
    matchLabels:
      app: keycloak
{{- end }}
