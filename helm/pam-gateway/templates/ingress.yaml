{{- if .Values.ingress.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: pam-gateway
  namespace: {{ .Values.namespace }}
spec:
  {{- if .Values.ingress.className }}
  ingressClassName: {{ .Values.ingress.className }}
  {{- end }}
  rules:
    - host: {{ .Values.ingress.host }}
      http:
        paths:
          - path: /api
            pathType: Prefix
            backend:
              service:
                name: pam-gateway-api
                port:
                  number: 80
          - path: /swagger
            pathType: Prefix
            backend:
              service:
                name: pam-gateway-api
                port:
                  number: 80
          {{- if .Values.observabilityStack.enabled }}
          - path: /grafana
            pathType: Prefix
            backend:
              service:
                name: grafana
                port:
                  number: {{ .Values.observabilityStack.grafana.service.port }}
          - path: /prometheus
            pathType: Prefix
            backend:
              service:
                name: prometheus
                port:
                  number: {{ .Values.observabilityStack.prometheus.service.port }}
          {{- end }}
          - path: /
            pathType: Prefix
            backend:
              service:
                name: pam-gateway-ui
                port:
                  number: 80
{{- end }}
